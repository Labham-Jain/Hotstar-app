export interface Theme {
  category: string;
  id: string;
  palette: {
    primary: string;
    secondary: string;
    accent: string;
    textFade: string;
    neutral: string;
    text: string;
  };
}
